	MOV R3, #10H
	MOV DPTR, #9000H
	CLR C
	MOVX A, @DPTR
	MOV R1, A
LOOP	INC DPTR
	MOVX A, @DPTR
	CLR C
	MOV R2, A
	SUBB A, R1
	JC SKIP
	MOV A, R2
	MOV R1, A
SKIP	DJNZ R3, LOOP
	MOV DPTR, #4000H
	MOV A, R1
	MOVX @DPTR, A
	LCALL 0003H
